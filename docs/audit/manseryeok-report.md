# 만세력 정밀도 감사 — 2026-07-03 (Phase 0)

> 하네스: [`scripts/audit/manseryeok-verify.ts`](../../scripts/audit/manseryeok-verify.ts)
> 재실행: `npx tsx scripts/audit/manseryeok-verify.ts`

---

## 1. 요약

| 지표 | 값 | 판정 |
|---|---|---|
| 총 케이스 | 1,112건 | — |
| 하드코딩 골든셋 (known-good) 일치 | 1 / 1 | ✅ |
| 무작위 1,000건 완주율 (구조적 8자 형식 통과) | 1,000 / 1,000 | ✅ |
| 경계 케이스 완주율 | 111 / 111 | ✅ |
| 예외·크래시 | 0 | ✅ |
| **정확도 % (KASI 기준 대조)** | **미측정** | 🟡 |

**결론**: 결정론 엔진 자체는 안정적이고 예외 없이 완주한다. 그러나 IMPLEMENTATION_PLAN.md가 요구하는 "정확도 100%"를 확정 짓기 위한 **외부 기준(KASI) 대조는 미완**이다. 원인은 Phase 0 부록 A의 첫 항목(OWNER 결정 3건 중 아직 미확정)과 KASI 데이터 확보 방침 미정이다.

---

## 2. 하네스 구성

### 2-1. 검증 계층
1. **하드코딩 골든셋 (`KNOWN_GOOD`, 1건)** — 기존 `src/utils/saju.test.ts`에서 확인된 케이스.
   - 입력: 음력 1969-10-23 10:00 → 예상 `[시주 癸巳, 일주 辛亥, 월주 乙亥, 년주 己酉]`.
   - 실측: 정확히 일치 ✅.
2. **경계 케이스 (`buildBoundaryJieqi`, 96건)** — 12절기 × 4개 연도(1990/2000/2010/2020) × ±30분(00:30, 23:30).
   - 판정 기준: 예외 없이 4주 8자를 반환하는지(구조적 완주성). 값 정확성은 KASI 대조 시 확정.
3. **야자시/조자시 경계 (`buildBoundaryYajasi`, 8건)** — 4개 대표일 × (23:30, 00:30).
4. **서머타임 경계 (`buildBoundaryDST`, 4건)** — 1948, 1957, 1987, 1988년 7월 15일.
5. **표준시 변경 경계 (`buildBoundaryStdTime`, 3건)** — 1912-01-01, 1954-03-21, 1961-08-10.
6. **무작위 1,000건 (`buildRandomCases`)** — 시드 고정(42) 결정론적 생성, 범위 1900~2030.

### 2-2. 데이터셋 확정 대기
`REFERENCE_DATASET` 배열 구조는 확정. KASI 확보 후 다음 스키마로 즉시 주입 가능:
```ts
{ id, category, input:{dateStr,timeStr,isLunar,isLeap}, expected:{year,month,day,hour}, note }
```

⛔ OWNER 결정 필요:
- **A안**: KASI Open API (한국천문연구원)에서 절기 시각을 매 연도 일괄 수집 → 자체 절기 테이블 구축 후 lunar-javascript 결과와 대조.
- **B안**: 공개 만세력 사이트(3~5곳)에서 500~1,000건 크롤링 후 상호 다수결 검증. 저작권·robots 준수 필요.
- **C안**: 외부 명리학 전문가에게 30~50건 검증 발주 → 소규모 정본 데이터셋 확보.

---

## 3. 유형별 실측 결과

### 3-1. 알려진 정답 (known-good)
| 케이스 | 입력 | 예상 | 실측 | 결과 |
|---|---|---|---|---|
| kg-001 | 음력 1969-10-23 10:00 | 시주=癸巳, 일주=辛亥, 월주=乙亥, 년주=己酉 | 동일 | ✅ 일치 |

### 3-2. 절입 경계 (96건)
- 결과: 96/96 완주. 크래시·NaN·빈 값 없음.
- **판정**: 값 정확성은 KASI 대조로 확정 예정. `expected` 부재로 mismatch 카운트에는 잡히지 않음(`no-expected` 상태).

### 3-3. 야자시 / 조자시 (8건)
- 결과: 8/8 완주.
- **정책 실측**: `saju.ts:293` `setDayZero(2)` 설정에 따라 `00:00~00:59`도 다음 날 자시로 계산되는 야자시 원칙이 적용된다.
- ⛔ OWNER 결정: 이 정책을 유지할지, 조자시(자정 이후는 당일 자시)로 전환할지 확정 필요. Phase 1 진입 전 필수.

### 3-4. 서머타임 (4건) — ⚠️ 구조적 결함 발견
- 결과: 4/4 완주.
- **⚠️ 결함**: `getAdjustedTime()`(saju.ts:245)이 -60분/-90분 오프셋을 정확히 계산하지만 **`getSajuData()`에서 호출되지 않는다**. 반환된 offset은 어디에도 적용되지 않은 채 유실된다.

`getAdjustedTime` 오프셋 실측 (하네스 출력):
| 날짜 | 반환 offset | 의미 | 실제 반영? |
|---|---|---|---|
| 1948-07-15 | -90분 | 표준시 -30 + DST -60 | ❌ 미반영 |
| 1957-07-15 | -60분 | DST | ❌ 미반영 |
| 1987-07-15 | -90분 | 표준시 -30 + DST -60 (실제로는 1987년 DST는 5~10월) | ❌ 미반영 |
| 1988-07-15 | -90분 | 동일 | ❌ 미반영 |
| 1912-01-01 | -30분 | GMT+8:30 시작 | ❌ 미반영 |
| 1954-03-21 | 0분 | GMT+9 복귀 | ❌ 미반영 (0이라 무해) |
| 1961-08-10 | -30분 | GMT+8:30 재도입 | ❌ 미반영 |
| 2020-06-15 | -30분 | ⚠️ **오류**. 2020년은 KST GMT+9. -30분이 반환됨 | ❌ 미반영 (반영되지 않는 게 다행) |

- `getAdjustedTime` 자체에도 버그: 조건 `dateNum >= 19610810` 이 1961-08-10 이후 **모든 시점**에 -30분을 적용한다. 실제로는 1961-08-10 ~ 1961-12-31만 GMT+8:30이었고 1962-01-01부터 GMT+9로 복귀함(문헌 참고).
- **⛔ OWNER 결정**: 서머타임/표준시 보정을 도입하는가? 도입한다면 어느 문헌을 정본으로 삼는가? Phase 1 `lib/manseryeok/policy.ts`에 반드시 명문화 필요.

### 3-5. 무작위 1,000건 (1900~2030)
- 결과: 1,000/1,000 완주. 예외·NaN·빈 값 없음.
- **판정**: 구조적 완주성 통과. 정확도는 KASI 대조 대기.
- 세부 시드: `seed=42` 고정. 재실행 시 동일 시퀀스 재현.

---

## 4. 발견된 실제 이슈 (Phase 1에서 반드시 해결)

### 4-1. `getAdjustedTime()` 오퍼번(orphan) — ✅ 2026-07-04 Phase 1-1에서 해소
- **파일**: `src/utils/saju.ts:245-265` (구), `src/lib/manseryeok/policy.ts` (신)
- **처리**: 정책 로직을 `src/lib/manseryeok/policy.ts::getKstNormalizationOffsetMinutes`로 이관. `getSajuData`/`getDaeunData`에서 실제 호출하도록 결합. 기존 `getAdjustedTime`은 audit 스크립트 호환용 얇은 wrapper로 유지.
- **정정된 조건**: GMT+8:30 기간은 1908-04-01~1911-12-31 및 1954-03-21~1961-08-09만 해당(+30분). DST 기간은 12개 정확한 일자 범위 상수화(-60분). 기존의 잘못된 조건("1961-08-10 이후 무한 -30분")과 오프셋 부호 오류 정정.
- **커버리지**: `src/lib/manseryeok/policy.test.ts` 15개 케이스로 각 정책 분기 검증.

### 4-2. 절입 경계 안내 플래그 부재 — 🟡 P1
- **위치**: `getSajuData` 반환값.
- **문제**: 출생 시각이 절기 시각 ±24시간 이내인 케이스에 대한 플래그가 없다. IMPLEMENTATION_PLAN.md §Phase 1-1이 요구.
- **Phase 1 대응**: 반환 객체에 `nearJieqiBoundary: boolean` 추가.

### 4-3. 진태양시 항상 미적용 — 🟡 P1
- **위치**: `getSajuData(...longitude?)` 시그니처.
- **문제**: `longitude` 인자가 있어야만 진태양시가 적용된다. 프론트 입력 흐름(`PremiumOrderForm.tsx`)에서 경도 입력을 노출하지 않는 한 사실상 미적용.
- **Phase 1 대응**: 정책 확정 후 서울 경도(126.98) 기본값 적용 or 미적용을 명문화.

### 4-4. 대운 시작 연령 반올림 정책 — 🟡 P2
- **위치**: `saju.ts:572` `Math.round(diffDays / 3)`.
- **관측**: 회귀 케이스에서 `daeunSu = 8`. 반올림 방식(round vs floor+분·시 재적용)은 문헌마다 다름.
- **Phase 1 대응**: `docs/decisions.md`에 결정 사항 명문화.

---

## 5. Phase 1 진입 판단

**정확도 % 확정 조건이 미충족**이므로 IMPLEMENTATION_PLAN.md Phase 1의 DoD("만세력 정확도 100%")를 지금 시점에 판정할 수 없다. Phase 1 착수 자체는 가능하나, 다음 순서로 진행한다.

1. **⛔ OWNER 결정 3건 접수**: 야자시·진태양시·서머타임/표준시 정책.
2. **KASI/외부 기준 데이터셋 확보 방침 결정** (A/B/C 위 3안 중 택일).
3. **`getAdjustedTime` 결함 수정** — 코드 접합 + 조건식 정정.
4. **골든셋 재실행** — 정확도 %가 확정되면 그때 DoD 판정.
5. **정확도 100% 미달 시 이 단계에서 중단하고 보고** (IMPLEMENTATION_PLAN.md §Phase 1-1 명시).

---

## 6. 재실행 방법

```bash
npx tsx scripts/audit/manseryeok-verify.ts
```

- 종료 코드: mismatch/error 존재 시 1, 전부 통과 시 0.
- 콘솔에 요약 + 불일치 케이스 상세 + `getAdjustedTime` 오프셋 실측을 출력.
- 하네스 확장 방법: `KNOWN_GOOD` 배열에 `{ id, category, input, expected, note }` 항목 추가.

---

## 7. 2026-07-04 후속: KASI 실데이터 확보 및 연·월주 절입 경계 버그 발견·수정

⛔ OWNER가 유효 KASI Open API 키를 발급(D-1-4 A안 실행). 이번 절에서 §2-2의 "데이터셋 확정 대기"가 해소되고, §5의 "확정 조건 미충족" 상태가 부분적으로 해소되었다.

### 7-1. KASI 절기 테이블 확보
- 신규: `scripts/kasi/fetch-jieqi.ts` — `get24DivisionsInfo`로 연도별 24절기 수집.
- 신규 데이터: `src/lib/manseryeok/data/kasi-jieqi.json` — **2000~2028년 696건** (실측 커버리지. 1999년 이하·2029년 이상은 API가 0건 반환).
- KASI 자체 데이터 결함 2건 발견·교정: 2000-02-19 절기명 오라벨(우수→입춘 오표기, 위치기반 자동교정), 2011 입동 시각 오류(09:26 표기, 독립 천문계산 대조 결과 실제 ≈03:34).
- 신규: `src/lib/manseryeok/jieqi-astro.ts` — Meeus 저정밀 태양황경 계산(+ΔT) 기반 절기 산출 모듈. KASI 불일치 tie-break 및 2000년 미만·2028년 초과 공백구간 감시용(분단위 1차 기준은 아님 — lunar-javascript가 KASI 대비 중앙값 0.27분로 더 정확).

### 7-2. 확정 버그 발견: 연·월주 절입 경계 조기 전환 (~28분)
- **원인**: `lunar-javascript`가 절기 시각을 **베이징시(UTC+8)** 로 반환하는데, 엔진은 출생시각에만 진태양시 보정(D-1-2)을 적용한 값을 그대로 먹여 절기(베이징 프레임)와 출생(서울 진태양시 프레임)을 다른 기준으로 비교하고 있었다.
- **실측**: `scripts/kasi/verify-month-boundary.ts`로 KASI 절(節) 348건 전수 경계 스캔 → 수정 전 **0/348 정확(±2분)**, 평균 −27.3분 이르게 전환.
- **영향 범위**: 연주(입춘 기준)·월주(각 절 기준) 모두. 2024 입춘(17:27) 사례: 실제 출생 17:12~17:27인 사람이 연주까지 틀리게 산출되고 있었다.
- **수정**: `src/utils/saju.ts`에 `getBeijingInstant()` 도입 — 출생 실제 순간을 베이징 벽시계로 환산해 절기와 동일 프레임에서 비교. `getSajuData`(연·월주)·`getDaeunData`(대운 방향·시작월주·대운수) 모두 이 기준으로 통일, 일·시주는 기존 진태양시 계산 유지.
- **수정 후 실측**: 347/347(100%, ±2분 이내, KASI 자체 오류 1건 제외).
- **커밋**: `cad29e1 fix(manseryeok): 연·월주 절입 경계 ~28분 조기전환 버그 수정 (Phase 1-1)`.

### 7-3. 갱신된 §1 요약 재판정
| 지표 | 값 | 판정 |
|---|---|---|
| 절입 경계(연·월주) 대 KASI 실측 정확도 | 347/347 (±2분) | ✅ 수정 완료 |
| 무작위 1,000건 대 KASI **8자 전수 대조** | ✅ §8에서 실행: 1,000/1,000 (100%) | ✅ 완료 |

**§5 DoD 재판정**: "정확도 100%"의 핵심 구성요소인 절입 경계 오류는 해소되었으나, IMPLEMENTATION_PLAN.md가 요구하는 "KASI 기준 1,000건 8자 전수 비교"는 아직 미실행이다. `scripts/audit/manseryeok-verify.ts`에 KASI 대조 모드를 통합하는 것이 다음 착수 항목이다(아래 §7-4). → **§8에서 실행 완료.**

### 7-4. 남은 항목
1. ~~`scripts/audit/manseryeok-verify.ts`에 KASI expected 값 주입~~ — ✅ §8-1 (100%).
2. ~~1900~1999·2029~2030 KASI 공백구간 검증 방침~~ — ✅ §8-2 (이상 없음, D-1-4 보강 기록).
3. ~~일주 자정 경계(진태양시 −32분 보정이 일주를 넘기는지) 별도 확인~~ — ✅ §8-1 자정 타깃 360건 (100%).
4. `getDaeunData` 전용 단위 테스트 보강(현재는 saju.test.ts 경계 케이스로 간접 검증됨).

---

## 8. 2026-07-04 후속 2차: KASI 대조 모드 통합 — DoD "1,000건 정확도 100%" 달성

### 8-1. KASI 기준 8자 전수 대조 (신규: `scripts/audit/kasi-expected.ts`)
- **방법**: 엔진(lunar-javascript)과 **완전 독립**으로 expected 8자를 산출해 대조.
  - 연·월주 경계: KASI 절입 시각(권위 기준). 간지 배정은 순수 산술(연간 `(Y−4) mod 10/12`, 오호둔).
  - 일주: 율리우스일(JDN) 60갑자 순환. 앵커 1949-10-01=甲子, 교차앵커(1900-01-01=甲戌, 2000-01-01=戊午) 자가검증.
  - 시주: 2시간 블록 + 오서둔. 야자시(23시대)는 익일 일간 기준(setSect(2) 유파와 동일).
  - 진태양시·표준시 정규화는 정책 정의이므로 policy.ts 산식을 재현(검증 대상 아님).
  - KASI 자체 오류인 2011 입동은 천문계산(jieqi-astro)으로 패치, 근접 ±45분 케이스는 제외.
- **결과** (`npx tsx scripts/audit/manseryeok-verify.ts`):

| 케이스군 | 건수 | 일치 | 정확도 |
|---|---|---|---|
| 절입 경계 타깃 (전체 절 ±5분·±6시간) | 1,388 | 1,388 | **100%** |
| 일주 자정/야자시 경계 타깃 (23:40~01:05 × 5개년 × 12개월) | 360 | 360 | **100%** |
| 무작위 1,000건 (2000~2028, 시드 결정론) | 1,000 | 1,000 | **100%** |

- **DoD 판정**: IMPLEMENTATION_PLAN.md Phase 1 "교차검증 1,000건 정확도 100%" **충족** (KASI 커버리지 2000~2028 내, 총 2,748건).
- 무작위 케이스 중 절입 ±5분 이내는 기준 분해상도(KASI 분 단위) 중첩으로 제외 후 재추출 — 이번 시드에서 제외 0건.

### 8-2. KASI 공백구간(1900~1999, 2029~2030) 방침 확정 (신규: `scripts/kasi/watch-gap-years.ts`)
- lunar-javascript vs 독립 천문계산(jieqi-astro) 절(節) **1,224건 전수 대조**: |오차| 평균 3.73분, p99 10.66분, 최대 13.92분 — 천문계산 자체 정밀도(±14분) 이내, 30분 임계 초과 **0건**.
- 결론: 공백구간에서도 lunar-javascript를 1차 기준으로 유지, 별도 데이터 발주 불필요. `docs/decisions.md` 2026-07-04 D-1-4 보강 항목으로 기록.

### 8-3. 야자시 이행 명세 정정 (동작 변화 없음)
- 종전 `setDayZero(2)` 호출(saju.ts, taekilEngine.ts, seoulDateGanji.ts 3곳)은 lunar-javascript에 **존재하지 않는 메서드**로 typeof 가드에 걸려 no-op이었고, 야자시 동작은 라이브러리 기본값(sect=2)에 우연히 의존하고 있었다.
- 실존 API `EightChar.setSect(2)`로 교체해 정책을 명시 고정. sect=2가 기본값이므로 산출 변화는 없음(vitest 91/91, 하네스 3,860건 재확인).

### 8-4. 잔여 항목 (Phase 1-1 후속)
1. ~~월운 12개 산출(절입 기준)~~ — ✅ §8-5.
2. 격국·용신 `SajuAnalysis` 스키마 편입.
3. `getDaeunData` 전용 단위 테스트 보강 + D-1-5(대운수 반올림 정책) 결정.

### 8-5. 월운(月運) 12개 산출 구현 (신규: `src/lib/manseryeok/wolun.ts`)
- `getWolunData(sajuYear)`: 입춘(Y)~입춘(Y+1)을 절(節) 기준 12개월(寅~丑)로 분할. 월간은 연간 오호둔, 각 달에 절입 시작/끝 시각(KST ISO + UTC ms) 포함 — 월운 캘린더 뷰(Phase 2-4)가 그대로 사용 가능.
- `getCurrentWolun(date)`: 조회 시점의 사주 연도(입춘 기준)·세운·현재 월운 산출 (IMPLEMENTATION_PLAN 1-1 "조회 시점 기준" 요구).
- `getSeunGanzhi(year)`: 세운 간지.
- 절입 시각은 lunar-javascript(D-1-4 1차 기준, 베이징→KST 환산) — 엔진 월주(`getBeijingInstant` 프레임)와 동일 기준이라 자동 정합.
- 테스트 9건(`wolun.test.ts`): 오호둔 시퀀스, 구간 연속 불변식, KASI 실측 ±2분 대조(2026년 13개 경계), **엔진 월주와 경계 ±5분 정합**, 입춘 전 1월의 전년도 귀속, KASI 커버리지 밖 연도(1970·2030) 완주. 전체 스위트 100/100.
